#!/usr/bin/env sh

# Run backend tests before push
# --runInBand prevents DB contention from parallel integration tests
# --retryTimes=1 handles flaky order-dependent tests that pass in isolation
cd backend
npm test -- --runInBand --retryTimes=1
