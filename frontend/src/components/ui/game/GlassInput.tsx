/**
 * GlassInput — Frosted glass text input (Story 22.6)
 *
 * Composites over input.tsx (celestial-input CSS class).
 * Visual: var(--glass-bg) background, var(--glass-border) border, gold focus ring,
 * var(--text-muted) placeholder.
 *
 * All colour values come from CSS custom property tokens — no raw hex or rgba literals.
 */
export { Input as GlassInput } from '@/components/ui/input';
export type { InputProps as GlassInputProps } from '@/components/ui/input';
