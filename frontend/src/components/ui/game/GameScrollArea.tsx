/**
 * GameScrollArea — Thin gold scrollbar scroll container (Story 22.6)
 *
 * Composites over scroll-area.tsx (scroll-area-celestial CSS class).
 * Visual: gold scrollbar thumb (--gold-dim, hover → --gold-primary), transparent track.
 */
export { ScrollArea as GameScrollArea } from '@/components/ui/scroll-area';
export type { ScrollAreaProps as GameScrollAreaProps } from '@/components/ui/scroll-area';
