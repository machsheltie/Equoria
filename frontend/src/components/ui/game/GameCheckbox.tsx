/**
 * GameCheckbox — Gold checkmark checkbox (Story 22.6)
 *
 * Composites over checkbox.tsx (Radix Checkbox).
 * Visual: gold checkmark SVG on check, navy bg (--celestial-navy-800),
 * --electric-blue-500 focus ring (via Radix data-state). Gold bg when checked.
 */
export { Checkbox as GameCheckbox } from '@/components/ui/checkbox';
