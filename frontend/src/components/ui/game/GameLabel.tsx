/**
 * GameLabel — Inter font label with optional small-caps and required-field variants (Story 22.6)
 *
 * Composites over label.tsx (Radix Label).
 * Visual: Inter font, --text-secondary color (default), caps variant (small-caps),
 * required variant (--gold-400 text).
 */
export { Label as GameLabel } from '@/components/ui/label';
