{
  "_doc": [
    "NARROW allowlist for backend APP-CODE $executeRawUnsafe / $queryRawUnsafe",
    "callsites (Equoria-jzu4l). The scanner check-no-unsafe-raw-sql.mjs FAILS on",
    "any unsafe-raw callsite in scanned app code (backend/**, excluding __tests__,",
    "tests, scripts, seed) that is NOT listed here. Each entry MUST carry a reason",
    "that justifies why the parameterized $executeRaw/$queryRaw tagged-template",
    "form is genuinely unavailable (true DDL with dynamic identifiers, or a static",
    "DDL literal). Adding a NEW entry to this list is a security-review decision,",
    "not a convenience — prefer migrating the callsite to a tagged template or a",
    "typed Prisma method. Keys are repo-relative POSIX paths.",
    "Format: each key maps to { reason, issue } (issue links the justification)."
  ],
  "allowlist": {
    "backend/services/databaseOptimizationService.mjs": {
      "reason": "True DDL — CREATE INDEX IF NOT EXISTS on an allowlisted column-name map. Index names and column identifiers cannot be bound parameters in SQL; identifiers are derived only from a fixed compile-time columnMapping (Object.hasOwn guard, no user input reaches the identifiers). Tagged templates bind VALUES, not identifiers, so they cannot express this DDL.",
      "issue": "Equoria-jzu4l"
    }
  }
}
