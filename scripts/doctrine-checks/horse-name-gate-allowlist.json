{
  "_doc": [
    "NARROW allowlist for request-reachable backend files that write horses.name",
    "WITHOUT importing the shared horse-name policy",
    "(backend/modules/horses/services/horseNamePolicy.mjs). Equoria-qkgfh.1.",
    "",
    "check-horse-name-gated.mjs FAILS on any such file that is not listed here.",
    "Every entry MUST carry a reason that says WHY the divergence is acceptable —",
    "either the value written is system-derived and provably inside the policy, or",
    "the divergence is a known behaviour question that belongs to the owner. A new",
    "entry is a decision, not a convenience: prefer routing the write through",
    "horseNameRejectionReason (a supplied name) or UNNAMED_HORSE_NAME (a newborn foal).",
    "",
    "This list may only SHRINK. An entry naming a file that no longer exists, or",
    "that no longer writes horses.name, FAILS the check — so a divergence cannot be",
    "grandfathered and then quietly forgotten.",
    "",
    "Keys are repo-relative POSIX paths. Format: { reason, issue }."
  ],
  "allowlist": {
    "backend/modules/auth/services/onboardingService.mjs": {
      "reason": "System-derived, no player string: writes `<username>'s First Horse` for the registration starter horse at REGISTRATION time. It can EXCEED the policy: username is validated 3-30 characters matching ^[a-zA-Z0-9_]+$ (authRoutes.mjs), so the result is 17-44 UTF-16 units and anything above a 26-character username is longer than the 40-character limit the owner set on 2026-09-14 (Equoria-zalyb). What IS guaranteed: no `<` and no NUL, and no player-typed string on this path. What is NOT: the length bound. Nothing breaks today — the bound is checked on input and this write never passes through a validator, the name is immediately renameable, and the onboarding form does not prefill it (OnboardingPage starts the field empty), so no new player can be rejected because of it. But the game does mint a name here that its own rule would refuse to accept back, and whether to shorten the generated name or widen nothing is the OWNER's call, not an implementer's (originally allow-listed under Equoria-qkgfh.1; the length gap was opened by the 40-character ruling). Do not read this entry as a guarantee that system-derived names satisfy the policy.",
      "issue": "Equoria-zalyb"
    },
    "backend/modules/marketplace/controllers/marketplaceController.mjs": {
      "reason": "System-derived, no player string: store horses are named `<Breed> #NNNNNN`. Breed names are DB-controlled (Breed.name is admin-created and uniquely indexed), not player input on this path.",
      "issue": "Equoria-qkgfh.1"
    },
    "backend/modules/users/services/gdprAccountService.mjs": {
      "reason": "System-derived, no player string: lineage anonymization rewrites a horse's name to `Anonymized Horse #<id>` during GDPR erasure. Always inside the policy by construction.",
      "issue": "Equoria-qkgfh.1"
    },
    "backend/modules/horses/services/horseModelService.mjs": {
      "reason": "The model-level insert (`createHorse`) that every acquisition path funnels through. It checks only `!name` and caps nothing itself, which is precisely the open door this check exists to keep visible: its callers are gated (or allow-listed) today, but a NEW caller could reach horses.name without meeting the policy. Deliberately left as an allow-list entry rather than gated in place, because clamping here would silently reshape a name a player supplied through a gated path — the normalisation the policy refuses. If this file is ever changed to validate, delete this entry.",
      "issue": "Equoria-qkgfh.1"
    },
    "backend/modules/horses/services/createHorseService.mjs": {
      "reason": "Service behind POST /api/v1/horses, which is CLOSED to players — the route returns 403 before any validation or body inspection runs (Finding 2 / Equoria-6p398.2). Not request-reachable today; its validator (validateHorseCreation) is gated on the shared rule anyway, so if the endpoint is ever reopened it reopens consistent. Delete this entry if the service is deleted with the route.",
      "issue": "Equoria-6p398.2"
    }
  }
}
