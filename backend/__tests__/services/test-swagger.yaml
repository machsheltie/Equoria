openapi: 3.0.3
info:
  title: Test API
  version: 1.0.0
  description: Test API specification
paths:
  /test:
    get:
      tags:
        - Test
      summary: Test endpoint
      description: A test endpoint
      responses:
        '200':
          description: Success
components:
  schemas:
    TestSchema:
      type: object
      properties:
        id:
          type: string
        name:
          type: string